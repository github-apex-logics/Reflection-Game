using UnityEngine;

public class LaserBeam2D : MonoBehaviour
{
    public float speed = 5f; // Speed at which the laser moves forward
    public float growthRate = 1f; // Rate at which the laser grows in size
    public float maxScale = 10f; // Maximum scale the laser can reach

    private void Update()
    {
        //this.gameObject.GetComponent<LineRenderer>().
    }
}
